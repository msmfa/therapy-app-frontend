node_modules
.expo/
.expo-shared/
.android/
.ios/
android/
ios/
.env
.env.production
dist/
build/
coverage/
*.log
TODO.md

.env.local

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Not the package manager for this project (yarn.lock is authoritative).
# EAS copies untracked files into the build, and two lock files make it
# ambiguous which package manager to use.
package-lock.json

# Local scratch/output, and Claude Code worktrees. These are untracked but
# were not ignored, so EAS was uploading them into the build context.
output/
tmp/
.claude/
